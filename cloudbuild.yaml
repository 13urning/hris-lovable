steps:
  # Build the Docker image
  - name: "gcr.io/cloud-builders/docker"
    args:
      - build
      - "-t"
      - "us-central1-docker.pkg.dev/$PROJECT_ID/wave-hris/app:$COMMIT_SHA"
      - "-t"
      - "us-central1-docker.pkg.dev/$PROJECT_ID/wave-hris/app:latest"
      - "."

  # Push both tags
  - name: "gcr.io/cloud-builders/docker"
    args:
      - push
      - "--all-tags"
      - "us-central1-docker.pkg.dev/$PROJECT_ID/wave-hris/app"

  # Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - run
      - deploy
      - wave-hris
      - "--image=us-central1-docker.pkg.dev/$PROJECT_ID/wave-hris/app:$COMMIT_SHA"
      - "--region=us-central1"
      - "--platform=managed"
      - "--quiet"

images:
  - "us-central1-docker.pkg.dev/$PROJECT_ID/wave-hris/app:$COMMIT_SHA"
  - "us-central1-docker.pkg.dev/$PROJECT_ID/wave-hris/app:latest"

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_HIGHCPU_8
